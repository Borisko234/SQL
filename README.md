# SQL
Simple SQL database schema for a restaurant online ordering system.

Includes tables for employees (waiters, cooks), tables, reservations, orders, and menu items, with proper foreign keys and relationships.

Bonus features:
- Transaction example for safe table reservation
- View for today’s reservations
- Trigger to free a table when a reservation is cancelled
- Indexes for better performance
